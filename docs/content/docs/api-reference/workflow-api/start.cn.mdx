---
title: 启动
---

启动/将新的工作流运行加入队列。

```typescript lineNumbers
import { start } from "workflow/api";
import { myWorkflow } from "./workflows/my-workflow";

const run = await start(myWorkflow); // [!code highlight]
```

## API 签名

### 参数

<TSDoc
definition={`
import { start } from "workflow/api";
export default start;`}
showSections={["parameters"]}
/>

#### StartOptions

<TSDoc
definition={`
import type { StartOptions } from "workflow/api";
export default StartOptions;`}
/>

### 返回

返回一个 `Run` 对象：

<TSDoc
definition={`
import { Run } from "workflow/api";
export default Run;`}
showSections={["returns"]}
/>

了解更多关于 [`WorkflowReadableStreamOptions`](/docs/api-reference/workflow-api/get-run#workflowreadablestreamoptions) 的信息。

## 注意事项

* 在运行时/非工作流上下文中使用 `start()` 函数以编程方式触发工作流执行。
* 这不同于直接调用工作流函数，后者是 Next.js 应用中的典型模式。
* 该函数在将工作流加入队列后立即返回 —— 它不会等待工作流完成。
* 所有参数必须是 [可序列化](/docs/foundations/serialization)。

## 示例

### 带参数

```typescript
import { start } from "workflow/api";
import { userSignupWorkflow } from "./workflows/user-signup";

const run = await start(userSignupWorkflow, ["user@example.com"]); // [!code highlight]
```

### 使用 `StartOptions`

```typescript
import { start } from "workflow/api";
import { myWorkflow } from "./workflows/my-workflow";

const run = await start(myWorkflow, ["arg1", "arg2"], { // [!code highlight]
  deploymentId: "custom-deployment-id" // [!code highlight]
}); // [!code highlight]
```