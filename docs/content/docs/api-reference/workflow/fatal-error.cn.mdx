---
title: FatalError
---

当在步骤中抛出 `FatalError` 时，表示该工作流不应重试该步骤，并将其标记为失败。

当你不希望某个特定步骤被重试时，应使用它。

```typescript lineNumbers
import { FatalError } from "workflow"

async function fallibleWorkflow() {
    "use workflow"
    await fallibleStep();
}

async function fallibleStep() {
    "use step"
    throw new FatalError("Fallible!") // [!code highlight]
}
```

## API 签名

### 参数

<TSDoc
definition={`
interface Error {
  /**

* The error message.
   */
  message: string;
}
export default Error;`}
/>