---
title: SvelteKit
description: 本指南将引导你在 SvelteKit 应用中设置第一个工作流。在此过程中，你将了解更多对在你自己的项目中使用该开发工具包至关重要的概念。
---

<Steps>

<Step>
## 创建你的 SvelteKit 项目

首先创建一个新的 SvelteKit 项目。此命令会创建一个名为 `my-workflow-app` 的新目录，包含最小化设置并在其中设置一个 SvelteKit 项目。

```bash
npx sv create my-workflow-app --template=minimal --types=ts --no-add-ons
```

进入新创建的目录：

```bash
cd my-workflow-app
```

### 安装 `workflow`

```package-install
npm i workflow
```

### 配置 Vite

在你的 Vite 配置中添加 `workflowPlugin()`。这将启用对 `"use workflow"` 和 `"use step"` 指令的使用。

```typescript title="vite.config.ts" lineNumbers
import { sveltekit } from "@sveltejs/kit/vite";
import { defineConfig } from "vite";
import { workflowPlugin } from "workflow/sveltekit"; // [!code highlight]

export default defineConfig({
  plugins: [sveltekit(), workflowPlugin()], // [!code highlight]
});
```

<Accordion type="single" collapsible>
  <AccordionItem value="typescript-intellisense" className="[&_h3]:my-0">
    <AccordionTrigger className="text-sm">
      ### 为 TypeScript 设置 IntelliSense（可选）
    </AccordionTrigger>
    <AccordionContent className="[&_p]:my-2">

要在你的 IDE 中启用有用的提示，请在 `tsconfig.json` 中设置 workflow 插件：

```json title="tsconfig.json" lineNumbers
{
  "compilerOptions": {
    // ... rest of your TypeScript config
    "plugins": [
      {
        "name": "workflow" // [!code highlight]
      }
    ]
  }
}
```

    </AccordionContent>
  </AccordionItem>
</Accordion>

</Step>

<Step>

## 创建你的第一个工作流

为我们的第一个工作流创建一个新文件：

```typescript title="workflows/user-signup.ts" lineNumbers
import { sleep } from "workflow";

export async function handleUserSignup(email: string) {
  "use workflow"; // [!code highlight]

  const user = await createUser(email);
  await sendWelcomeEmail(user);

  await sleep("5s"); // Pause for 5s - doesn't consume any resources
  await sendOnboardingEmail(user);

  console.log("Workflow is complete! Run 'npx workflow web' to inspect your run")

  return { userId: user.id, status: "onboarded" };
}

```

我们接下来会填充那些函数，但先看看这段代码：

* 我们使用指令 `"use workflow"` 定义了一个 **workflow** 函数。可以把 **workflow** 函数看作各个 **steps** 的 _编排者_。
* Workflow DevKit 的 `sleep` 函数允许我们在不消耗任何资源的情况下暂停 workflow 的执行。一个 sleep 可以持续几秒、几小时、几天，甚至几个月。

## 创建你的工作流步骤

现在我们来定义那些缺失的函数。

```typescript title="workflows/user-signup.ts" lineNumbers
import { FatalError } from "workflow"

// Our workflow function defined earlier

async function createUser(email: string) {
  "use step"; // [!code highlight]

  console.log(`Creating user with email: ${email}`);

  // Full Node.js access - database calls, APIs, etc.
  return { id: crypto.randomUUID(), email };
}

async function sendWelcomeEmail(user: { id: string; email: string; }) {
  "use step"; // [!code highlight]

  console.log(`Sending welcome email to user: ${user.id}`);

  if (Math.random() < 0.3) {
  // By default, steps will be retried for unhandled errors
   throw new Error("Retryable!");
  }
}

async function sendOnboardingEmail(user: { id: string; email: string}) {
  "use step"; // [!code highlight]

  if (!user.email.includes("@")) {
    // To skip retrying, throw a FatalError instead
    throw new FatalError("Invalid Email");
  }

  console.log(`Sending onboarding email to user: ${user.id}`);
}
```

看看这段代码：

* 业务逻辑位于 **steps** 内。当在 **workflow** 中调用一个 step 时，它会被排队以在单独的请求中运行，同时 workflow 会被挂起，就像 `sleep` 一样。
* 如果 step 抛出错误（例如在 `sendWelcomeEmail` 中），该 step 将自动重试，直到成功为止（或达到该 step 的最大重试次数）。
* 如果错误是有意的且不应重试，steps 可以抛出 `FatalError`。

<Callout>
我们将在 [基础](/docs/foundations) 中深入探讨 workflows、steps 以及挂起或处理事件的其他方式。
</Callout>

</Step>

<Step>

## 创建你的路由处理器

要调用你的新 workflow，我们需要将其添加到一个 `POST` API 路由处理器 `src/routes/api/signup/+server.ts` 中，代码如下：

```typescript title="src/routes/api/signup/+server.ts"
import { start } from "workflow/api";
import { handleUserSignup } from "../../../../workflows/user-signup";
import { json, type RequestHandler } from "@sveltejs/kit";

export const POST: RequestHandler = async ({
  request,
}: {
  request: Request;
}) => {
  const { email } = await request.json();

  // Executes asynchronously and doesn't block your app
  await start(handleUserSignup, [email]);

  return json({ message: "User signup workflow started" });
};

```

该路由处理器在 `/api/signup` 创建了一个 `POST` 请求端点，用于触发你的 workflow。

<Callout>
Workflows 可以从 API 路由或任何服务端代码触发。
</Callout>

</Step>

</Steps>

## 在开发环境运行

要启动开发服务器，在 SvelteKit 根目录的终端运行以下命令：

```bash
npm run dev
```

一旦开发服务器运行，你可以在终端运行以下命令触发你的 workflow：

```bash
curl -X POST --json '{"email":"hello@example.com"}' http://localhost:5173/api/signup
```

查看 SvelteKit 开发服务器日志，以查看你的 workflow 执行情况以及正在处理的 steps。

此外，你还可以使用 [Workflow DevKit CLI 或 Web UI](/docs/observability) 来详细检查你的 workflow 运行和 steps。

```bash
# Open the observability Web UI
npx workflow web
# or if you prefer a terminal interface, use the CLI inspect command
npx workflow inspect runs
```

<img src="/o11y-ui.png" alt="Workflow DevKit Web UI" />

## 部署到生产环境

Workflow DevKit 应用目前在部署到 [Vercel](https://vercel.com/home) 时表现最佳，且不需要特殊配置。

查看 [部署](/docs/deploying) 部分以了解如何在其他地方部署你的 workflows。

## 下一步

* 了解更多关于 [基础](/docs/foundations) 的内容。
* 如果遇到问题，请查看 [错误](/docs/errors)。
* 查看 [API 参考](/docs/api-reference)。